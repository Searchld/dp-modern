/**
 * 溜井数字化监控管理平台 - 应用入口
 * 
 * @description 创建 Vue 应用实例，挂载全局样式和根组件
 * @author zhangzhen
 * @version 0.1.0
 */
import { createApp } from 'vue'
import App from './App.vue'
import './styles/base.css'

// 创建 Vue 应用并挂载到 #app 节点
createApp(App).mount('#app')
